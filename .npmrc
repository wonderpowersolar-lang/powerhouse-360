# Flat, npm-like node_modules. The website depends on three/@react-three and
# Next's standalone tracing, which are most reliable without pnpm's symlinked
# strict layout. Keeps behaviour close to the previous npm setup.
node-linker=hoisted
auto-install-peers=true
