# WP-1.3-Kern — Immobilien-Objektbaum Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Der app-relevante Kern des Immobilien-Objektbaums (Property→Building→Entrance→Unit + Address) existiert als migriertes Datenmodell mit idempotentem Pilotstruktur-Seed für den ADR-006-Testmandanten (1 Property, 2 Gebäude, 21 Units), AccessScope als reinem Datenmodell-Stub und einer minimalen Admin-Lesesicht `/admin/objects` hinter der bestehenden WP-1.2-Auth.

**Architecture:** Das Datenmodell folgt Spec §3.1 (Kunden-App-Architekturplan) und dem Masterplan-Zielbild §5: eigene UUID-Tabellen mit `organizationId`-Mandantenanker auf `Property` (ADR-004), Gebäudeadresse als eigenes `Address`-Modell, natürliche Unique-Constraints (`[organizationId, name]`, `[propertyId, name]`, `[buildingId, label]`) machen den Seed upsert-idempotent. `AccessScope` wird ausschließlich als Tabelle angelegt (keine Guard-Integration — die gehört zum WP-1.3-Rest). Die Lesesicht ist eine Server Component, die über `getAuthContext` (WP-1.2) und die neue Permission `object.read` membership-gescoped liest; Mitglieder einer POWERHOUSE-Org sehen mandantenübergreifend (Masterplan §4 Nr. 4).

**Tech Stack:**
- Prisma 6 (Client-Output `packages/database/generated/client`, Re-Export via `@ph360/database`), PostgreSQL 16 (Dev :5433 via docker-compose)
- Next.js 16.2.7 App Router (`apps/platform`, Port 3100), React 19, Server Components
- Vitest 3 — Unit (`packages/permissions`) + Integration gegen `ph360_test` (:5433) via `packages/testing` (WP-1.2 Task 3)
- pnpm 11.11.0 + Turborepo, TS strict (ES2022, moduleResolution bundler, verbatimModuleSyntax)

**Vorbedingungen:** WP-1.2 ist komplett umgesetzt (Gates F-02/F-19/F-20 grün). Konkret wird vorausgesetzt:
1. Prisma-Migration `auth_and_rbac` angewendet; Modelle `User`, `Session`, `Account`, `Verification`, `OrganizationMembership`, `Invitation` + Enums `SystemRole`, `MembershipStatus`, `InvitationStatus` existieren in `packages/database/prisma/schema.prisma`.
2. `packages/auth` (`@ph360/auth`) exportiert `getAuthContext(headers: Headers)`, `requirePermission(ctx, perm, { organizationId })`, `assertOrgScope`, `AuthnError`, `AuthzError` und den Typ `AuthContext` (`{ userId, email, name, memberships: { organizationId, role }[] }`).
3. `packages/testing` (`@ph360/testing`) existiert mit `createOrg(type, overrides?)`, `truncateAll`, `src/global-setup.ts`, `src/setup.ts`; Root-`vitest.config.ts` hat die Projekte `unit` (Include `packages/permissions/**/*.{test,spec}.ts`) und `integration` (Include `packages/**/*.itest.ts`, `apps/**/*.itest.ts`, globalSetup = migrate deploy auf `DATABASE_URL_TEST`); `.env` enthält `DATABASE_URL_TEST=postgresql://…:5433/ph360_test`; Root-Scripts `test:unit`, `test:integration`, `db:test:up` existieren.
4. `apps/platform` hat `@ph360/auth`, `@ph360/permissions` als Dependencies, die Session-Middleware auf `/admin/*` (Redirect → `/login`), die Admin-Shell `src/app/admin/layout.tsx` mit Navigation und das Bootstrap-Script `pnpm ph360:create-admin` (WP-1.2 Task 11).
5. WP-1.2-V2-Delta: Der Seed legt ggf. bereits einen Testmandanten (Organization mit Namenspräfix `TEST — `) an. Der Pilotstruktur-Seed dieses Plans **findet und verwendet** einen solchen Mandanten wieder (findFirst auf Namenspräfix) und legt ihn nur an, wenn er fehlt — es entsteht nie ein zweiter Testmandant.

**NICHT im Scope (bleibt beim ursprünglichen WP-1.3-Rest):** CRM-Ausbau (Customer/Opportunity/Note/Task), Lead-Qualifizierung, CSV-Import, Zoho-Adapter, AccessScope-Guard-Integration, IssuingEntity-Pflichtfelder, `Floor`/`Room`/`TechnicalRoom`/`GridConnection`.

---

## Dateistruktur

**Neu**
- `packages/database/prisma/migrations/*_immobilien_kern/migration.sql` — von Prisma generierte Migration für die 6 neuen Tabellen + Enum.
- `packages/database/prisma/seed-objects.ts` — idempotenter Pilotstruktur-Seed (`seedPilotStructure`), von `seed.ts` und Tests aufgerufen.
- `packages/database/src/immobilien.itest.ts` — Integrationstests: Objektbaum anlegen, Uniques, Cascades, AccessScope-Stub.
- `packages/database/prisma/seed-pilot.itest.ts` — Integrationstests: Seed-Idempotenz + Testmandanten-Kennzeichnung.
- `packages/testing/src/objects-factories.itest.ts` — Integrationstest der neuen Objekt-Factories.
- `packages/permissions/src/object-read.test.ts` — Unit-Tests für die neue Permission `object.read` (Positiv + Negativmatrix).
- `apps/platform/src/lib/objects.ts` — Lese-Service: Org-Sichtbarkeitsauflösung + Property-Baum-Query (`getReadablePropertyTree`).
- `apps/platform/src/lib/objects.itest.ts` — Integrationstests: Membership-Scoping, deny-by-default, POWERHOUSE-Plattformsicht.
- `apps/platform/src/app/admin/objects/page.tsx` — Server Component: Tabelle Property→Building→Unit.

**Geändert**
- `packages/database/prisma/schema.prisma` — + Abschnitt „immobilien“ (Address, Property, Building, Entrance, Unit, AccessScope, Enum AccessScopeType) + 2 Org-Rückrelationen.
- `packages/database/prisma/seed.ts` — ruft `seedPilotStructure` nach dem Powerhouse-Org-Seed auf.
- `packages/testing/src/factories.ts` — + `createProperty`, `createBuilding`, `createEntrance`, `createUnit`.
- `packages/permissions/src/permissions.ts` — + `"object.read"` im Katalog.
- `packages/permissions/src/roles.ts` — `object.read` für PLATFORM_ADMIN (via ALL), OPERATIONS, PROPERTY_MANAGER.
- `apps/platform/src/app/admin/layout.tsx` — + Nav-Link „Objekte“ → `/admin/objects`.
- `apps/platform/package.json` — + devDependency `@ph360/testing` (für den lib-Integrationstest).
- `docs/POWERHOUSE_360_MASTER_PLAN.md`, `docs/EXECUTION_ROADMAP.md`, `docs/IMPLEMENTATION_LOG.md` — Statuspflege (Task 6).

---

## Task 1: Prisma-Schema Immobilien-Kern + AccessScope-Stub + Migration `immobilien_kern`

**Files:**
- Modify: `packages/database/prisma/schema.prisma`
- Test: `packages/database/src/immobilien.itest.ts`

- [ ] **Step 1: Fehlschlagenden Integrationstest schreiben**

`packages/database/src/immobilien.itest.ts` (nutzt bewusst kein `@ph360/testing`, um keinen Workspace-Zyklus database↔testing zu erzeugen — Orgs werden direkt via Prisma angelegt; Truncate zwischen Tests kommt aus dem globalen `setupFiles` der Integration-Suite):

```ts
import { describe, it, expect } from "vitest";
import { prisma } from "@ph360/database";

let n = 0;
function makeOrg(type: "WEG" | "PROPERTY_MANAGER" = "WEG") {
  return prisma.organization.create({
    data: { type, name: `${type} ${Date.now()}-${n++}` },
  });
}

async function makeTree() {
  const org = await makeOrg();
  const property = await prisma.property.create({
    data: { organizationId: org.id, name: "Testobjekt" },
  });
  const address = await prisma.address.create({
    data: { street: "Christinenstraße", houseNumber: "36", postalCode: "10119", city: "Berlin" },
  });
  const building = await prisma.building.create({
    data: { propertyId: property.id, name: "Christinenstraße 36", addressId: address.id },
  });
  const entrance = await prisma.entrance.create({
    data: { buildingId: building.id, label: "Haupteingang" },
  });
  const unit = await prisma.unit.create({
    data: { buildingId: building.id, entranceId: entrance.id, label: "WE 01", floor: 0 },
  });
  return { org, property, address, building, entrance, unit };
}

describe("Immobilien-Kern — Schema (WP-1.3-Kern)", () => {
  it("erzeugt Property → Building(+Address) → Entrance → Unit", async () => {
    const { property, unit } = await makeTree();
    const tree = await prisma.property.findUniqueOrThrow({
      where: { id: property.id },
      include: { buildings: { include: { address: true, entrances: true, units: true } } },
    });
    expect(tree.buildings).toHaveLength(1);
    expect(tree.buildings[0]!.address.postalCode).toBe("10119");
    expect(tree.buildings[0]!.entrances[0]!.label).toBe("Haupteingang");
    expect(tree.buildings[0]!.units[0]!.id).toBe(unit.id);
    expect(tree.buildings[0]!.units[0]!.floor).toBe(0);
  });

  it("verhindert doppelte Unit-Labels je Gebäude und doppelte Property-Namen je Org", async () => {
    const { org, property, building } = await makeTree();
    await expect(
      prisma.unit.create({ data: { buildingId: building.id, label: "WE 01" } }),
    ).rejects.toThrow();
    await expect(
      prisma.property.create({ data: { organizationId: org.id, name: property.name } }),
    ).rejects.toThrow();
  });

  it("Property-Löschung kaskadiert auf Buildings/Entrances/Units", async () => {
    const { property } = await makeTree();
    await prisma.property.delete({ where: { id: property.id } });
    expect(await prisma.building.count()).toBe(0);
    expect(await prisma.entrance.count()).toBe(0);
    expect(await prisma.unit.count()).toBe(0);
  });

  it("Entrance-Löschung setzt Unit.entranceId auf null (Unit bleibt)", async () => {
    const { entrance, unit } = await makeTree();
    await prisma.entrance.delete({ where: { id: entrance.id } });
    const reloaded = await prisma.unit.findUniqueOrThrow({ where: { id: unit.id } });
    expect(reloaded.entranceId).toBeNull();
  });

  it("AccessScope-Stub: Property-Scope für eine fremde Org (HV) ist anlegbar", async () => {
    const { property } = await makeTree();
    const hv = await makeOrg("PROPERTY_MANAGER");
    const scope = await prisma.accessScope.create({
      data: { organizationId: hv.id, scopeType: "PROPERTY", propertyId: property.id },
    });
    expect(scope.scopeType).toBe("PROPERTY");
    expect(scope.buildingId).toBeNull();
  });
});
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/database/src/immobilien.itest.ts
```
Erwartet: alle 5 Tests FAIL mit `TypeError: Cannot read properties of undefined (reading 'create')` (es gibt noch kein `prisma.property`).

- [ ] **Step 3: Schema erweitern**

In `packages/database/prisma/schema.prisma` im bestehenden `model Organization { … }` unter den vorhandenen Rückrelationen (nach `auditEvents AuditEvent[]` bzw. den WP-1.2-Zeilen `memberships`/`invitations`) ergänzen:

```prisma
  properties   Property[]
  accessScopes AccessScope[]
```

Am Dateiende einen neuen Abschnitt anhängen:

```prisma
// ---------------------------------------------------------------------------
// immobilien — Objektbaum-Kern (WP-1.3-Kern; Spec §3.1 Kunden-App-Architekturplan)
// Floor/Room/TechnicalRoom/GridConnection folgen mit dem WP-1.3-Rest.
// ---------------------------------------------------------------------------

model Address {
  id          String @id @default(uuid()) @db.Uuid
  street      String
  houseNumber String
  postalCode  String
  city        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  buildings Building[]

  @@map("address")
}

model Property {
  id             String       @id @default(uuid()) @db.Uuid
  organizationId String       @db.Uuid
  organization   Organization @relation(fields: [organizationId], references: [id])
  name           String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  buildings    Building[]
  accessScopes AccessScope[]

  @@unique([organizationId, name])
  @@index([organizationId])
  @@map("property")
}

model Building {
  id         String   @id @default(uuid()) @db.Uuid
  propertyId String   @db.Uuid
  property   Property @relation(fields: [propertyId], references: [id], onDelete: Cascade)
  name       String
  addressId  String   @db.Uuid
  address    Address  @relation(fields: [addressId], references: [id])
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  entrances    Entrance[]
  units        Unit[]
  accessScopes AccessScope[]

  @@unique([propertyId, name])
  @@map("building")
}

model Entrance {
  id         String   @id @default(uuid()) @db.Uuid
  buildingId String   @db.Uuid
  building   Building @relation(fields: [buildingId], references: [id], onDelete: Cascade)
  label      String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  units Unit[]

  @@unique([buildingId, label])
  @@map("entrance")
}

model Unit {
  id         String    @id @default(uuid()) @db.Uuid
  buildingId String    @db.Uuid
  building   Building  @relation(fields: [buildingId], references: [id], onDelete: Cascade)
  entranceId String?   @db.Uuid
  entrance   Entrance? @relation(fields: [entranceId], references: [id], onDelete: SetNull)
  label      String
  floor      Int? // 0 = EG
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@unique([buildingId, label])
  @@index([buildingId])
  @@map("unit")
}

enum AccessScopeType {
  PROPERTY
  BUILDING
}

/// WP-1.3-Kern: reiner Datenmodell-Stub (ADR-004, Masterplan §4). Die
/// begünstigte Organisation (z. B. HV) erhält expliziten, auditierbaren
/// Teilbaum-Zugriff. Guard-Integration ist bewusst NICHT Teil dieses WP
/// (WP-1.3-Rest); bis dahin wertet kein Codepfad diese Tabelle aus.
model AccessScope {
  id             String          @id @default(uuid()) @db.Uuid
  organizationId String          @db.Uuid
  organization   Organization    @relation(fields: [organizationId], references: [id])
  scopeType      AccessScopeType
  propertyId     String?         @db.Uuid
  property       Property?       @relation(fields: [propertyId], references: [id], onDelete: Cascade)
  buildingId     String?         @db.Uuid
  building       Building?       @relation(fields: [buildingId], references: [id], onDelete: Cascade)
  grantedById    String? // User-ID (better-auth String-ID); im Stub bewusst ohne Relation
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  @@index([organizationId])
  @@map("access_scope")
}
```

- [ ] **Step 4: Migration erzeugen + Client regenerieren**

```bash
docker compose up -d --wait postgres
set -a; . ./.env; set +a
pnpm --filter @ph360/database exec prisma migrate dev --name immobilien_kern
pnpm --filter @ph360/database exec prisma migrate status
```
Erwartet: Migration `*_immobilien_kern/migration.sql` erzeugt und angewendet (SQL enthält `CREATE TABLE` für `address`, `property`, `building`, `entrance`, `unit`, `access_scope` + `CREATE TYPE "AccessScopeType"`); Client regeneriert; Status „Database schema is up to date“.

- [ ] **Step 5: Test grün**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/database/src/immobilien.itest.ts
```
Erwartet: 5 Tests PASS (das globalSetup der Integration-Suite wendet die neue Migration per `migrate deploy` auf `ph360_test` an).

- [ ] **Step 6: Typecheck + Commit**

```bash
pnpm --filter @ph360/database typecheck
git add packages/database/prisma/schema.prisma packages/database/prisma/migrations packages/database/src/immobilien.itest.ts
git commit -m "feat(immobilien): Objektbaum-Kern Property/Building/Entrance/Unit/Address + AccessScope-Stub (Migration immobilien_kern)"
```

---

## Task 2: Objekt-Factories in `packages/testing`

**Files:**
- Modify: `packages/testing/src/factories.ts`
- Test: `packages/testing/src/objects-factories.itest.ts`

- [ ] **Step 1: Fehlschlagenden Integrationstest schreiben**

`packages/testing/src/objects-factories.itest.ts`:

```ts
import { describe, it, expect } from "vitest";
import { prisma } from "@ph360/database";
import { createOrg, createProperty, createBuilding, createEntrance, createUnit } from "./index.js";

describe("Objekt-Factories (WP-1.3-Kern)", () => {
  it("erzeugt einen vollständigen Objektbaum mit Defaults", async () => {
    const org = await createOrg("WEG");
    const property = await createProperty(org.id);
    const building = await createBuilding(property.id);
    const entrance = await createEntrance(building.id);
    const unit = await createUnit(building.id, { entranceId: entrance.id, floor: 2 });

    const loaded = await prisma.property.findUniqueOrThrow({
      where: { id: property.id },
      include: { buildings: { include: { address: true, entrances: true, units: true } } },
    });
    expect(loaded.organizationId).toBe(org.id);
    expect(loaded.buildings).toHaveLength(1);
    expect(loaded.buildings[0]!.address.city).toBe("Berlin");
    expect(loaded.buildings[0]!.units[0]!.id).toBe(unit.id);
    expect(loaded.buildings[0]!.units[0]!.floor).toBe(2);
    expect(loaded.buildings[0]!.units[0]!.entranceId).toBe(entrance.id);
  });

  it("Defaults kollidieren nicht (zwei Units ohne Label-Override im selben Gebäude)", async () => {
    const org = await createOrg("WEG");
    const property = await createProperty(org.id);
    const building = await createBuilding(property.id);
    await createUnit(building.id);
    await createUnit(building.id);
    expect(await prisma.unit.count({ where: { buildingId: building.id } })).toBe(2);
  });
});
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/testing/src/objects-factories.itest.ts
```
Erwartet: FAIL mit `SyntaxError`/`TypeError` — `createProperty` wird nicht aus `./index.js` exportiert.

- [ ] **Step 3: Factories implementieren**

An `packages/testing/src/factories.ts` anhängen (die Datei importiert bereits `prisma` und definiert `uniq()` — WP-1.2 Task 3; die Typen kommen aus dem regenerierten Client):

```ts
import type { Property, Building, Entrance, Unit } from "@ph360/database";

export function createProperty(
  organizationId: string,
  overrides: { name?: string } = {},
): Promise<Property> {
  return prisma.property.create({
    data: { organizationId, name: overrides.name ?? `Property ${uniq()}` },
  });
}

export async function createBuilding(
  propertyId: string,
  overrides: { name?: string; street?: string; houseNumber?: string } = {},
): Promise<Building> {
  const address = await prisma.address.create({
    data: {
      street: overrides.street ?? "Teststraße",
      houseNumber: overrides.houseNumber ?? "1",
      postalCode: "10119",
      city: "Berlin",
    },
  });
  return prisma.building.create({
    data: { propertyId, name: overrides.name ?? `Gebäude ${uniq()}`, addressId: address.id },
  });
}

export function createEntrance(
  buildingId: string,
  overrides: { label?: string } = {},
): Promise<Entrance> {
  return prisma.entrance.create({
    data: { buildingId, label: overrides.label ?? `Eingang ${uniq()}` },
  });
}

export function createUnit(
  buildingId: string,
  overrides: { label?: string; entranceId?: string; floor?: number } = {},
): Promise<Unit> {
  return prisma.unit.create({
    data: {
      buildingId,
      label: overrides.label ?? `WE ${uniq()}`,
      entranceId: overrides.entranceId ?? null,
      floor: overrides.floor ?? null,
    },
  });
}
```

Hinweis: Der Typ-Import gehört an den Dateianfang zu den bestehenden Imports (`import type { … } from "@ph360/database"` mit den vorhandenen Typ-Imports zusammenführen, verbatimModuleSyntax verlangt `import type`). `packages/testing/src/index.ts` exportiert bereits `export * from "./factories.js"` — kein weiterer Export nötig.

- [ ] **Step 4: Test grün**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/testing/src/objects-factories.itest.ts
```
Erwartet: 2 Tests PASS.

- [ ] **Step 5: Commit**

```bash
git add packages/testing/src
git commit -m "test(immobilien): Objekt-Factories createProperty/createBuilding/createEntrance/createUnit"
```

---

## Task 3: Idempotenter Pilotstruktur-Seed (ADR-006-Testmandant)

**Files:**
- Create: `packages/database/prisma/seed-objects.ts`
- Modify: `packages/database/prisma/seed.ts`
- Test: `packages/database/prisma/seed-pilot.itest.ts`

- [ ] **Step 1: Fehlschlagenden Integrationstest schreiben**

`packages/database/prisma/seed-pilot.itest.ts`:

```ts
import { describe, it, expect } from "vitest";
import { prisma } from "@ph360/database";
import { seedPilotStructure } from "./seed-objects.js";

describe("Pilotstruktur-Seed (ADR-006, WP-1.3-Kern)", () => {
  it("legt 1 Property, 2 Gebäude, 21 Units an und ist idempotent", async () => {
    const first = await seedPilotStructure(prisma);
    const second = await seedPilotStructure(prisma); // zweiter Lauf: keine Duplikate
    expect(second.propertyId).toBe(first.propertyId);
    expect(second.organizationId).toBe(first.organizationId);

    expect(await prisma.property.count()).toBe(1);
    expect(await prisma.building.count()).toBe(2);
    expect(await prisma.entrance.count()).toBe(2);
    expect(await prisma.unit.count()).toBe(21);

    const buildings = await prisma.building.findMany({
      include: { address: true, units: true },
      orderBy: { name: "asc" },
    });
    expect(buildings.map((b) => b.name)).toEqual(["Christinenstraße 36", "Lottumstraße 22"]);
    expect(buildings[0]!.units).toHaveLength(11);
    expect(buildings[1]!.units).toHaveLength(10);
    expect(buildings[0]!.address.street).toBe("Christinenstraße");
    expect(buildings[0]!.address.postalCode).toBe("10119");
    expect(buildings[1]!.address.street).toBe("Lottumstraße");
    // Jede Unit hängt am Haupteingang ihres Gebäudes
    for (const b of buildings) for (const u of b.units) expect(u.entranceId).not.toBeNull();
  });

  it("verwendet einen als TEST gekennzeichneten Mandanten und legt keinen zweiten an", async () => {
    const preexisting = await prisma.organization.create({
      data: { type: "WEG", name: "TEST — Pilot Christinenstraße" },
    });
    const { organizationId } = await seedPilotStructure(prisma);
    expect(organizationId).toBe(preexisting.id);
    const org = await prisma.organization.findUniqueOrThrow({ where: { id: organizationId } });
    expect(org.name.startsWith("TEST — ")).toBe(true);
    expect(await prisma.organization.count({ where: { name: { startsWith: "TEST — " } } })).toBe(1);
  });
});
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/database/prisma/seed-pilot.itest.ts
```
Erwartet: FAIL — `Cannot find module './seed-objects.js'`.

- [ ] **Step 3: Seed-Modul implementieren**

`packages/database/prisma/seed-objects.ts`:

```ts
import type { PrismaClient } from "../generated/client/index.js";

/** ADR-006: dauerhafter, klar gekennzeichneter Testmandant („TEST — …“). */
export const TEST_TENANT_NAME = "TEST — Pilot Christinenstraße";

/**
 * Pilotstruktur (Masterplan §1: WEG Christinenstraße 36 / Lottumstraße 22,
 * Berlin, 21 Messstellen/Einheiten). Dokumentierte Annahme bis zum
 * Pilot-CSV-Realimport (WP-1.3-Rest): Verteilung 11/10 Units, 2 WE je Etage.
 * Der Realimport korrigiert Verteilung/Etagen; Summe 21 ist verbindlich.
 */
const PILOT = {
  propertyName: "WEG Christinenstraße 36 / Lottumstraße 22",
  buildings: [
    { name: "Christinenstraße 36", street: "Christinenstraße", houseNumber: "36", postalCode: "10119", city: "Berlin", unitCount: 11 },
    { name: "Lottumstraße 22", street: "Lottumstraße", houseNumber: "22", postalCode: "10119", city: "Berlin", unitCount: 10 },
  ],
} as const;

/**
 * Idempotent: Wiederverwendung eines vorhandenen Testmandanten (Namenspräfix
 * „TEST — “, z. B. aus dem WP-1.2-V2-Delta-Seed); alle Strukturknoten per
 * upsert über ihre natürlichen Unique-Keys. Beliebig oft ausführbar.
 */
export async function seedPilotStructure(
  prisma: PrismaClient,
): Promise<{ organizationId: string; propertyId: string }> {
  const org =
    (await prisma.organization.findFirst({ where: { name: { startsWith: "TEST — " } } })) ??
    (await prisma.organization.create({ data: { type: "WEG", name: TEST_TENANT_NAME } }));

  const property = await prisma.property.upsert({
    where: { organizationId_name: { organizationId: org.id, name: PILOT.propertyName } },
    update: {},
    create: { organizationId: org.id, name: PILOT.propertyName },
  });

  for (const spec of PILOT.buildings) {
    let building = await prisma.building.findUnique({
      where: { propertyId_name: { propertyId: property.id, name: spec.name } },
    });
    if (!building) {
      const address = await prisma.address.create({
        data: { street: spec.street, houseNumber: spec.houseNumber, postalCode: spec.postalCode, city: spec.city },
      });
      building = await prisma.building.create({
        data: { propertyId: property.id, name: spec.name, addressId: address.id },
      });
    }

    const entrance = await prisma.entrance.upsert({
      where: { buildingId_label: { buildingId: building.id, label: "Haupteingang" } },
      update: {},
      create: { buildingId: building.id, label: "Haupteingang" },
    });

    for (let i = 1; i <= spec.unitCount; i++) {
      const label = `WE ${String(i).padStart(2, "0")}`;
      await prisma.unit.upsert({
        where: { buildingId_label: { buildingId: building.id, label } },
        update: {},
        create: {
          buildingId: building.id,
          entranceId: entrance.id,
          label,
          floor: Math.floor((i - 1) / 2), // 0 = EG; Annahme 2 WE je Etage (s. o.)
        },
      });
    }
  }

  return { organizationId: org.id, propertyId: property.id };
}
```

- [ ] **Step 4: `seed.ts` erweitern**

In `packages/database/prisma/seed.ts` den Import ergänzen und `main()` erweitern. Zielzustand von `main()` (der Powerhouse-Block bleibt unverändert; falls WP-1.2 dort bereits Testmandant-/IssuingEntity-Blöcke ergänzt hat, bleiben diese ebenfalls stehen — es kommt nur der `seedPilotStructure`-Aufruf ans Ende):

```ts
import { PrismaClient } from "../generated/client/index.js";
import { seedPilotStructure } from "./seed-objects.js";

const prisma = new PrismaClient();

/**
 * Idempotent seed: ensures the Powerhouse 360 tenant exists. This is the
 * organization that receives inbound leads (WP-1.1). Re-runnable.
 * WP-1.3-Kern: + Pilotstruktur im ADR-006-Testmandanten.
 */
async function main() {
  const existing = await prisma.organization.findFirst({
    where: { type: "POWERHOUSE" },
  });

  const org =
    existing ??
    (await prisma.organization.create({
      data: { type: "POWERHOUSE", name: "AKL Powerhouse 360 GmbH" },
    }));

  console.log(`[seed] Powerhouse organization: ${org.id} (${org.name})`);

  const pilot = await seedPilotStructure(prisma);
  console.log(
    `[seed] Pilotstruktur im Testmandanten ${pilot.organizationId}: Property ${pilot.propertyId} (2 Gebäude, 21 Units)`,
  );
}

main()
  .then(() => prisma.$disconnect())
  .catch(async (err) => {
    console.error("[seed] failed:", err);
    await prisma.$disconnect();
    process.exit(1);
  });
```

- [ ] **Step 5: Test grün + Seed gegen Dev-DB verifizieren**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration packages/database/prisma/seed-pilot.itest.ts
```
Erwartet: 2 Tests PASS. Danach Dev-DB:

```bash
set -a; . ./.env; set +a
pnpm db:seed
pnpm db:seed
```
Erwartet: beide Läufe erfolgreich, identische IDs in der `[seed] Pilotstruktur …`-Zeile (Idempotenz auch gegen die Dev-DB).

- [ ] **Step 6: Typecheck + Commit**

```bash
pnpm --filter @ph360/database typecheck
git add packages/database/prisma
git commit -m "feat(immobilien): idempotenter Pilotstruktur-Seed — Testmandant, 2 Gebäude (Christinenstraße 36 / Lottumstraße 22), 21 Units (ADR-006)"
```

---

## Task 4: Permission `object.read` (TDD)

**Files:**
- Modify: `packages/permissions/src/permissions.ts`, `packages/permissions/src/roles.ts`
- Test: `packages/permissions/src/object-read.test.ts`

- [ ] **Step 1: Fehlschlagenden Unit-Test schreiben**

`packages/permissions/src/object-read.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { SYSTEM_ROLES, roleHasPermission, resolvePermissions } from "./index.js";

const ALLOWED = ["PLATFORM_ADMIN", "OPERATIONS", "PROPERTY_MANAGER"] as const;

describe("object.read (WP-1.3-Kern)", () => {
  it("erlaubt PLATFORM_ADMIN, OPERATIONS und PROPERTY_MANAGER", () => {
    for (const role of ALLOWED) {
      expect(roleHasPermission(role, "object.read")).toBe(true);
    }
  });

  it("verweigert allen übrigen Rollen (deny-by-default, F-20)", () => {
    const allowed = new Set<string>(ALLOWED);
    for (const role of SYSTEM_ROLES) {
      if (allowed.has(role)) continue;
      expect(roleHasPermission(role, "object.read")).toBe(false);
    }
  });

  it("RESIDENT bleibt ohne Permissions (App-Permissions kommen erst mit WP-APP-2)", () => {
    expect(resolvePermissions(["RESIDENT"]).size).toBe(0);
  });
});
```

- [ ] **Step 2: Test ausführen — muss fehlschlagen**

```bash
pnpm --filter @ph360/permissions test
```
Erwartet: die 2 neuen Positiv-Assertions FAIL (`expected false to be true`) — `object.read` existiert noch nicht; die WP-1.2-Bestandstests bleiben grün.

- [ ] **Step 3: Katalog + Rollen erweitern**

`packages/permissions/src/permissions.ts` — im `PERMISSIONS`-Array nach `"organization.read"` ergänzen:

```ts
  "object.read",
```

`packages/permissions/src/roles.ts` — (a) im `ALL`-Array `"object.read"` ergänzen (PLATFORM_ADMIN erhält es damit automatisch), (b) in `ROLE_PERMISSIONS` die beiden Zeilen ändern zu:

```ts
  OPERATIONS: ["lead.read", "member.read", "organization.read", "object.read"],
```

```ts
  PROPERTY_MANAGER: ["lead.read", "organization.read", "object.read"],
```

- [ ] **Step 4: Tests grün + Typecheck**

```bash
pnpm --filter @ph360/permissions test
pnpm --filter @ph360/permissions typecheck
```
Erwartet: alle Unit-Tests PASS (Bestand + 3 neue), Typecheck ohne Fehler.

- [ ] **Step 5: Commit**

```bash
git add packages/permissions/src
git commit -m "feat(auth): Permission object.read — Objektbaum-Lesesicht für PLATFORM_ADMIN/OPERATIONS/PROPERTY_MANAGER"
```

---

## Task 5: Admin-Lesesicht `/admin/objects` (Lese-Service TDD + Server Component)

**Files:**
- Create: `apps/platform/src/lib/objects.ts`, `apps/platform/src/app/admin/objects/page.tsx`
- Modify: `apps/platform/src/app/admin/layout.tsx`, `apps/platform/package.json`
- Test: `apps/platform/src/lib/objects.itest.ts`

- [ ] **Step 1: `@ph360/testing` als devDependency der Platform ergänzen**

In `apps/platform/package.json` unter `devDependencies` ergänzen (nur falls WP-1.2 Task 12 das nicht schon getan hat):

```json
"@ph360/testing": "workspace:*"
```

Dann: `pnpm install`.

- [ ] **Step 2: Fehlschlagenden Integrationstest schreiben**

`apps/platform/src/lib/objects.itest.ts`:

```ts
import { describe, it, expect } from "vitest";
import type { AuthContext } from "@ph360/auth";
import { createOrg, createProperty, createBuilding, createUnit } from "@ph360/testing";
import { getReadablePropertyTree, readablePropertyOrgIds } from "./objects";

function ctx(memberships: AuthContext["memberships"]): AuthContext {
  return { userId: "u-test", email: "t@example.test", name: "Test", memberships };
}

describe("Objekt-Lesesicht — Scoping (Deklaration: permission object.read)", () => {
  it("OPERATIONS sieht nur Properties der eigenen Organisation (F-02)", async () => {
    const orgA = await createOrg("WEG");
    const orgB = await createOrg("WEG");
    const mine = await createProperty(orgA.id);
    await createProperty(orgB.id);

    const tree = await getReadablePropertyTree(
      ctx([{ organizationId: orgA.id, role: "OPERATIONS" }]),
    );
    expect(tree).toHaveLength(1);
    expect(tree[0]!.id).toBe(mine.id);
  });

  it("RESIDENT sieht nichts — deny-by-default (F-20)", async () => {
    const org = await createOrg("WEG");
    await createProperty(org.id);
    const c = ctx([{ organizationId: org.id, role: "RESIDENT" }]);
    expect(await readablePropertyOrgIds(c)).toEqual([]);
    expect(await getReadablePropertyTree(c)).toEqual([]);
  });

  it("ohne Membership: leeres Ergebnis", async () => {
    const org = await createOrg("WEG");
    await createProperty(org.id);
    expect(await getReadablePropertyTree(ctx([]))).toEqual([]);
  });

  it("PLATFORM_ADMIN einer POWERHOUSE-Org sieht mandantenübergreifend (Masterplan §4 Nr. 4)", async () => {
    const powerhouse = await createOrg("POWERHOUSE");
    const weg = await createOrg("WEG");
    const property = await createProperty(weg.id);
    const building = await createBuilding(property.id);
    await createUnit(building.id, { label: "WE 01" });

    const c = ctx([{ organizationId: powerhouse.id, role: "PLATFORM_ADMIN" }]);
    expect(await readablePropertyOrgIds(c)).toBeNull(); // null = plattformweite Sicht

    const tree = await getReadablePropertyTree(c);
    const found = tree.find((p) => p.id === property.id);
    expect(found).toBeDefined();
    expect(found!.buildings[0]!.units.map((u) => u.label)).toContain("WE 01");
    expect(found!.organization.name).toBe(weg.name);
  });

  it("OPERATIONS in einer Nicht-POWERHOUSE-Org erhält KEINE Plattformsicht", async () => {
    const hv = await createOrg("PROPERTY_MANAGER");
    const weg = await createOrg("WEG");
    await createProperty(weg.id);
    const c = ctx([{ organizationId: hv.id, role: "OPERATIONS" }]);
    expect(await readablePropertyOrgIds(c)).toEqual([hv.id]);
    expect(await getReadablePropertyTree(c)).toEqual([]);
  });
});
```

- [ ] **Step 3: Test ausführen — muss fehlschlagen**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration apps/platform/src/lib/objects.itest.ts
```
Erwartet: FAIL — `Cannot find module './objects'`.

- [ ] **Step 4: Lese-Service implementieren**

`apps/platform/src/lib/objects.ts`:

```ts
import { prisma } from "@ph360/database";
import { canAny } from "@ph360/permissions";
import type { AuthContext } from "@ph360/auth";

/**
 * Org-Sichtbarkeit der Objekt-Lesesicht. Routen-Deklaration: permission
 * `object.read`, Scope-Quelle = OrganizationMembership (ADR-004).
 * Rückgabe:
 * - `null`  → plattformweite Sicht: Membership mit object.read in einer
 *             POWERHOUSE-Org (Masterplan §4 Nr. 4 — Plattform-Mitarbeiter
 *             mandantenübergreifend per Systemrolle)
 * - `[]`    → keine Sicht (deny-by-default)
 * - `[ids]` → genau diese Mandanten
 */
export async function readablePropertyOrgIds(ctx: AuthContext): Promise<string[] | null> {
  const permitted = ctx.memberships
    .filter((m) => canAny([m.role], "object.read"))
    .map((m) => m.organizationId);
  if (permitted.length === 0) return [];
  const powerhouse = await prisma.organization.findFirst({
    where: { id: { in: permitted }, type: "POWERHOUSE" },
    select: { id: true },
  });
  return powerhouse ? null : permitted;
}

/** Vollständiger Lesebaum Property→Building(+Address,+Entrances)→Units. */
export async function getReadablePropertyTree(ctx: AuthContext) {
  const orgIds = await readablePropertyOrgIds(ctx);
  if (orgIds !== null && orgIds.length === 0) return [];
  return prisma.property.findMany({
    where: orgIds === null ? {} : { organizationId: { in: orgIds } },
    orderBy: { name: "asc" },
    include: {
      organization: { select: { name: true } },
      buildings: {
        orderBy: { name: "asc" },
        include: {
          address: true,
          entrances: true,
          units: { orderBy: { label: "asc" } },
        },
      },
    },
  });
}

export type PropertyTree = Awaited<ReturnType<typeof getReadablePropertyTree>>[number];
```

- [ ] **Step 5: Test grün**

```bash
pnpm db:test:up && pnpm exec vitest run --project integration apps/platform/src/lib/objects.itest.ts
```
Erwartet: 5 Tests PASS.

- [ ] **Step 6: Server Component `/admin/objects`**

`apps/platform/src/app/admin/objects/page.tsx`:

```tsx
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { getAuthContext } from "@ph360/auth";
import { getReadablePropertyTree } from "../../../lib/objects";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export const metadata = { title: "Objekte — Powerhouse 360" };

export default async function ObjectsPage() {
  const ctx = await getAuthContext(await headers());
  if (!ctx) redirect(`/login?next=${encodeURIComponent("/admin/objects")}`);

  const properties = await getReadablePropertyTree(ctx);

  return (
    <main className="wrap">
      <h1>Objekte</h1>
      <p className="muted">
        Immobilien-Objektbaum (Property → Gebäude → Einheit) · Lesesicht
      </p>

      {properties.length === 0 ? (
        <div className="empty">
          Keine Objekte sichtbar. Entweder existieren noch keine Immobilien in
          deinen Organisationen oder deine Rolle hat die Berechtigung
          „object.read“ nicht.
        </div>
      ) : (
        properties.map((property) => (
          <section key={property.id}>
            <h2>
              {property.name}{" "}
              <span className="muted">({property.organization.name})</span>
            </h2>
            {property.buildings.map((building) => (
              <div key={building.id}>
                <h3>
                  {building.name}
                  <span className="muted">
                    {" "}
                    — {building.address.street} {building.address.houseNumber},{" "}
                    {building.address.postalCode} {building.address.city} ·{" "}
                    {building.units.length}{" "}
                    {building.units.length === 1 ? "Einheit" : "Einheiten"}
                  </span>
                </h3>
                <table>
                  <thead>
                    <tr>
                      <th>Einheit</th>
                      <th>Etage</th>
                      <th>Eingang</th>
                    </tr>
                  </thead>
                  <tbody>
                    {building.units.map((unit) => (
                      <tr key={unit.id}>
                        <td>{unit.label}</td>
                        <td>{unit.floor === null ? "—" : unit.floor === 0 ? "EG" : `${unit.floor}. OG`}</td>
                        <td>
                          {building.entrances.find((e) => e.id === unit.entranceId)
                            ?.label ?? "—"}
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            ))}
          </section>
        ))
      )}
    </main>
  );
}
```

- [ ] **Step 7: Nav-Link in der Admin-Shell**

In `apps/platform/src/app/admin/layout.tsx` (WP-1.2 Task 8) in der Navigation direkt nach dem bestehenden Eintrag für `/admin/leads` denselben Link-Elementtyp mit diesem Ziel einfügen — je nach dort verwendetem Element exakt eine dieser beiden Zeilen:

```tsx
<a href="/admin/objects">Objekte</a>
```

```tsx
<Link href="/admin/objects">Objekte</Link>
```

- [ ] **Step 8: Typecheck + Commit**

```bash
pnpm --filter @ph360/platform typecheck
git add apps/platform/src apps/platform/package.json pnpm-lock.yaml
git commit -m "feat(immobilien): Admin-Lesesicht /admin/objects — Property→Building→Unit hinter object.read"
```

---

## Task 6: Gesamtsuite, Browser-Verifikation, Doku-/Statuspflege

**Files:**
- Modify: `docs/POWERHOUSE_360_MASTER_PLAN.md`, `docs/EXECUTION_ROADMAP.md`, `docs/IMPLEMENTATION_LOG.md`

- [ ] **Step 1: Volle Testsuite + Typecheck**

```bash
pnpm test:unit
pnpm test:integration
pnpm -r typecheck
```
Erwartet: alles grün (Unit: permissions inkl. `object.read`; Integration: WP-1.2-Bestand + `immobilien.itest`, `seed-pilot.itest`, `objects-factories.itest`, `objects.itest`).

- [ ] **Step 2: Browser-Verifikation des Nutzerflusses (Statusregel Masterplan §12)**

```bash
docker compose up -d --wait postgres
set -a; . ./.env; set +a
pnpm db:seed
ADMIN_EMAIL=admin@powerhouse360.de ADMIN_PASSWORD='ChangeMe-123!' pnpm ph360:create-admin
pnpm --filter @ph360/platform dev
```
Im Browser: `http://localhost:3100/admin/objects` unauthentifiziert → Redirect `/login`; nach Login als Admin → Seite zeigt 1 Property „WEG Christinenstraße 36 / Lottumstraße 22“ (mit Testmandanten-Namen), 2 Gebäude mit Adressen (10119 Berlin) und 11 + 10 = 21 Einheiten inkl. Etagen/Eingang. Nav-Link „Objekte“ sichtbar. Dev-Server danach stoppen.

- [ ] **Step 3: Doku-/Statuspflege**

1. `docs/IMPLEMENTATION_LOG.md`: append-only-Eintrag „2026-07-XX — WP-1.3-Kern: Immobilien-Objektbaum (Property/Building/Entrance/Unit/Address, Migration `immobilien_kern`), AccessScope-Datenmodell-Stub (ohne Guard-Integration), idempotenter Pilotstruktur-Seed (Testmandant ADR-006: 1 Property, 2 Gebäude, 21 Units), Permission `object.read`, Admin-Lesesicht `/admin/objects`; Suite grün, Browser-Flow verifiziert. Nicht enthalten (WP-1.3-Rest): CRM, CSV-Import, Zoho, AccessScope-Guards, IssuingEntity-Pflicht.“
2. `docs/POWERHOUSE_360_MASTER_PLAN.md` §10, Zeile „WP-1.3 Immobilien + CRM“: Status auf 🟡 und Zusatz „Kern (Objektbaum+Seed+Lesesicht) ✅ 2026-07 — Rest (CRM/CSV-Import/Zoho/AccessScope-Guards) offen“.
3. `docs/EXECUTION_ROADMAP.md` Abschnitt „WP-1.3“: die erste Prisma-Zeile als teilerledigt kennzeichnen („Kern erledigt: Property/Building/Entrance/Unit/Address + AccessScope-Tabelle; Floor/Room/TechnicalRoom/GridConnection + Guard-Integration offen“) und die Admin-UI-Zeile um „Objektliste (Lesesicht) ✅“ ergänzen.
4. Gate-Hinweis in beiden Dokumenten: F-03 bleibt offen (gehört zum WP-1.3-Rest: Lead→Kunde/Objekt); WP-APP-1 ist durch WP-1.3-Kern entblockt (Spec §8: „WP-APP-1 ← WP-1.3-Kern“).

- [ ] **Step 4: Commit**

```bash
git add docs/POWERHOUSE_360_MASTER_PLAN.md docs/EXECUTION_ROADMAP.md docs/IMPLEMENTATION_LOG.md
git commit -m "docs: WP-1.3-Kern abgeschlossen — Statuspflege Masterplan/Roadmap/Log"
```

---

## Abschluss-Checkliste

- [ ] **Gate-Verifikation:** `pnpm test:unit` und `pnpm test:integration` grün; F-02/F-20-Bestandstests unverändert grün; Scope-Negativtests der Lesesicht (RESIDENT/„keine Membership“/Nicht-POWERHOUSE) grün; F-03 ausdrücklich als **offen** dokumentiert (WP-1.3-Rest).
- [ ] **Seed-Idempotenz real geprüft:** `pnpm db:seed` zweimal gegen die Dev-DB gelaufen, identische IDs, keine Duplikate (1 Property / 2 Buildings / 2 Entrances / 21 Units, genau 1 Organization mit Präfix „TEST — “).
- [ ] **Browser-Flow durchlaufen** (Login → `/admin/objects` → Baum sichtbar) — Statusregel: 🟢 nur nach tatsächlich durchlaufenem Nutzerfluss.
- [ ] **AccessScope:** Tabelle existiert, wird von keinem Guard/Codepfad ausgewertet (bewusster Stub, im Schema-Kommentar dokumentiert).
- [ ] **IMPLEMENTATION_LOG-Eintrag** append-only ergänzt (Task 6 Step 3 Nr. 1).
- [ ] **Masterplan-Statuspflege** (§10 WP-1.3 auf 🟡 mit Kern-✅-Vermerk) + **EXECUTION_ROADMAP** nachgeführt; Entblockung von WP-APP-1 vermerkt.
- [ ] Alle Commits lokal (Conventional Commits deutsch), **kein `git push`** (kein Remote, R-02).
